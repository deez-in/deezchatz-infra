---
- name: Ensure user systemd container directory exists
  ansible.builtin.file:
    path: /home/rocky/.config/containers/systemd
    state: directory
    mode: '0755'

- name: Create Nijhum Podman Network Quadlet
  ansible.builtin.copy:
    dest: /home/rocky/.config/containers/systemd/nijhum.network
    content: |
      [Network]
      NetworkName=nijhum
    mode: '0644'
